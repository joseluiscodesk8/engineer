import styles from "../../styles/index.module.scss";

const Ready = () => {
  return (
    <section className={styles.ready}>
    <h2>ready to start saving?</h2>
    <p>Submit a project proposal to get started today!</p>
    <button>Get started</button>
  </section>
  );
};

export default Ready;
